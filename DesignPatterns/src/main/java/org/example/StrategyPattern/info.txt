Strategy Pattern
- defines a family of algorithms
- encapsulates each one and makes them interchangeable
- lets the algorithm vary independently of clients that use it (easily changeable)
- IS-A, HAS-A, Implement
